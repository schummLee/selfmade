#include <pthread.h>

noreturn _syscall_call_return_kernel_thread_kill_or_suspend();


// thanks to Amith32_joi posting the new frame-standrad so we can
// modified new session and avert old ones

extern void initalized_info();

extern inline bool is_return();



